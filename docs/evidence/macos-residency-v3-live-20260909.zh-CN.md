# 子代理驻留 v3：Mac 修复与原生验收记录

2026-09-09。最终 `030332` 已在 Mac 实装并通过本方案完整验收，原任务修补责任已结清。方案使用插件和已有原生工具，没有新增宿主接口、提高名额或重开业务任务。最终结果见末节“最终 Mac 验收完成”。

完整机器记录见[验收 JSON](macos-residency-v3-live-20260909.json)，对应[修复方案](../HOST-CAPACITY-LIFECYCLE-REPAIR-PLAN.zh-CN.md)。下文按时间保留各候选的通过、缺口和修正；旧节中的“尚未通过”“待信任”属于当时状态。模型用例均使用各自当时受信的定义，不混用不同候选的信任与测试证据。

## 已执行的候选验收

候选 `0.4.0+codex.20260908170449`，核心源码摘要 `39cbfde872ecdac10dff593df5c1ff0393adb8014b24d62f166a394ca9b1a64e`，Mac 安装与源码一致。

| 检查 | 实际结果与边界 |
| --- | --- |
| 全量本地检查 | 398 项：397 通过，0 失败，1 项 Windows 专属检查跳过 |
| 策略一致性 | 232 案例，0 不匹配；不是模型质量测量 |
| 同根 100 个普通阶段 | 1 次资格检查、100 个业务测试阶段，101 个唯一 outcome；50 次补充、5 次最后响应窗口对照；679 次原生调用，零容量拒绝 |
| 历史裁剪与占用 | 最终保留 64 条 attempt、101 条 managed child，追加计量 12,832,768 字节；不删除日志以换取容量 |
| 真实模型质量 | 18 个根控制回合、7 组用例：自检、补充与遗漏纠正、运行中追加、未结束操作取消收尾、缓存代理工具守卫、取消、延期恢复 |
| 真实满额恢复 | 根加三个子代理的既有额度；三个原生压力子代理各有未处理消息，出现精确拒绝；处理一个旧要求后，同根同阶段的新票据成功创建，失败 outcome 仅一次，其余要求也处理完毕 |
| 临时进程清理 | 上述三个驱动均确认自己拥有的进程组已退出 |

100 阶段和满额对照使用固定模型响应，但 Router、受信 Hook、原生创建/跟进和结果记录均真实执行。压力样本是明确标注的非 Router 原生控制子代理；该对照证明实际容量恢复路径，不冒充已迁移的旧 Router 数据。结果质量由另外执行的真实模型用例核验。

真实模型验收促成了两处修补：资格检查先读取旧 Stop 标记，误拒当前完整收尾；代码模式外层返回完成时，内部进程仍在运行但未被跟踪。后者当时只覆盖固定的单一转发写法；下文新增对照证明普通代码模式仍会漏判，不能将当时的通过扩大到所有写法。

## 原 Hosted 任务仍未通过

“完成 Hosted 验收并上线生产”的回合 `01a08201-1d06-7293-8820-8c52c5f2dd6c` 确实执行了只读检查。任务查询工具返回了空的回合投影；随后从该任务的原生日志读取到了真实工具调用和最终回复，因此没有把空投影当作未执行，也没有重复发送业务操作。

- 三名旧子代理的原生身份及原 attempt/outcome 均存在，但 `manage_stage` 恢复身份时用了 MCP 缓存目录，误拒业务任务目录。
- 原任务路由返回 `continue / HOST_LIFECYCLE_ROUND_TRIP_UNPROVEN`，没有新建子代理。已证明的旧 Hook 缓存使用旧匹配范围，现有比较规则阻止了当前已审阅扩展后的重新自检。
- 新候选改为从原生任务读取并验证目录、根任务和项目归属；仅允许已审阅的 Hook 匹配范围扩展进入新的无工具自检，不复用旧成功或跳过往返验证。97 项针对性检查通过。

这次原任务验收没有修改业务文件、部署、迁移、数据库写入或外部消息。原结果与旧 outcome 保留。下一步必须安装兼容修补、在原任务处理历史责任并完成真实新委派，再完成独立候选审查；这些项目未通过前不能标记 P4 或“本机完整恢复”。

## 兼容修补后的第二轮

`0.4.0+codex.20260908173426` 已安装，400 项本地测试通过、1 项 Windows 专属检查跳过；232 项策略检查通过。同根 100 个普通阶段再次通过，101 个唯一 outcome、675 次原生调用、零容量拒绝；核心源码摘要为 `bf220751f3a9329404dc922a2a35d26cdadeb294adbf143ba53b64e1bf1dd7f3`。

原任务回合 `01a0821a-f37d-7991-bf4f-8e91d0a13906` 已执行重验。旧身份恢复可以进入，第三名 child 的消息对账成功；其余对账及维护因主任务日志正在追加而返回 `native transcript changed during evidence read`。新修补对父/发送者消息对账采用固定前缀及二次摘要验证；60 项相关检查通过，尚须安装和原任务重验。

该轮新路由还受到插件全局 `ROUTER_CHILD_STORAGE_LIMIT` 阻止。原生名额没有改变，也没有发出新票据；预算的准确数值、推荐方案和用户待决项已列入[修复方案](../HOST-CAPACITY-LIFECYCLE-REPAIR-PLAN.zh-CN.md)。不会通过清除其它任务预留或临时修改预算让这项验收通过。

## 活动日志追加修补后的第三轮

候选 `0.4.0+codex.20260908175013` 已完成 Mac 原位安装和托管来源刷新，核心源码摘要为 `be6b1688e9006fbc88f1aa36d01fab422ac4e8791598b875122ec1a7f575494f`。404 项本地检查中 403 通过、1 项 Windows 专属检查跳过；232 项离线策略检查零不匹配。父任务对账只接受读取起点的完整前缀，并二次验证其字节未变；子结果默认仍要求整个文件稳定。前缀覆盖、截断和路径替换的负对照均拒绝。

该候选的 7 组真实模型用例全部通过，18 个根控制回合；实际满额恢复对照通过。同根连续 100 个普通阶段也通过：101 个唯一 outcome、698 次原生调用、零容量拒绝、12,910,592 字节追加计量。三个驱动均证实其拥有的进程组已退出，实际安装和源码摘要一致。这些是 `175013` 的运行结果，不能代表后续候选。

原任务回合 `01a0822e-aafe-7db0-881b-f9b706bfe2fc` 已完成三名旧子代理的逐一维护，原生任务状态为 completed。Kuhn、Ptolemy、Avicenna 的维护分别在 revision 25、3、2 核验，原 outcome 保持不变；两项业务补充要求延期保留给该根任务，不冒充已实现。Ptolemy 的升级前缓存守卫负对照实际执行成功，未观察到 PreToolUse 拒绝；临时文件已精确清理。这是守卫失败，后续纯文本收集及维护 verified 不能覆盖它。

随后的只读预算查询显示其它任务自行结算后，pending 从 3 降至 2，usedBytes 为 264,859,648，现行策略暂时允许下一份预留。未清除任何其它任务记录、未修改预算。原任务在历史维护完成后按真实新路由的实时准入结果继续独立审查；该计数观察本身不证明后续创建成功，也不撤销预算设计的用户待决项。

实际再次路由时，`router-v3-independent-readonly-review-20260909` 仍返回 `continue / ROUTER_CHILD_STORAGE_LIMIT`，没有票据、子代理或 outcome。独立审查尚未执行。

## 旧 MCP 进程兼容修补与当前守卫失败

候选 `0.4.0+codex.20260908183527` 已在 Mac 安装，核心源码摘要 `e857f6580fee2f643ab1313a7502a315b56392402244d6e180474cf8e6dfd4b1`。405 项本地测试：404 通过、0 失败、1 项 Windows 专属跳过；232 个策略案例零不匹配。此前 100 阶段、真实模型和满额对照的结果仍归属于各自旧候选，不能改记为本候选通过。

旧 MCP 内存中保留了严格 schema 比较函数；仅在磁盘覆盖 bootstrap 不会改变它。新增可选的 `manage_stage.senderTranscriptPaths` 因此导致新运行时不断被隔离。持久旧进程的回归测试先复现失败，再验证插件导出旧进程认可的、严格限定的兼容视图；内部调用保留完整新 schema，未审阅的必填变化仍拒绝。实际原 MCP 已返回 runtime/active/requested 全为新候选，activePointerStatus=matched，历史 8 条隔离记录未删除。

在这个真实热升级后，本修复任务的旧 Erdos 又执行了一次唯一 canary：原生回执 `chunk_id=51217a`、`exit_code=0`，确实写入指定临时文件。根任务核对内容后精确清理，确认文件不存在。Erdos 收集的原八项审查、后续三项审查均保留；当前维护 revision 2 已核验，守卫失败转为本根任务的未完成责任。原 passed outcome 的前后 SHA-256 相同。这证明 MCP 热升级修好，并不等于旧 child 的新 Hook 守卫已生效。

当前任务在直接执行 `list_agents` 后仍无本轮 PreToolUse 回执。原生源码的工具入口读取已加载的 Hook；旧 PreToolUse 匹配范围仅覆盖 spawn。这是旧规则未刷新的有力线索。补充对照使用当前候选、新的临时原生任务及 Desktop 同样的 `features.code_mode_host=true`，已实际通过：4 个真实模型控制回合、资格检查与缓存 child 维护守卫；原生日志记录的是 `custom_tool_call / exec`，内部命令被 PreToolUse 拒绝，原 outcome 不变、没有新 SubagentStart、所拥有的进程组已退出。这排除了代码模式入口必然绕过新守卫的解释，但不能替代升级前原任务缓存的验收。

Codex 现有插件变更处理已经实现加载任务的 Hook 刷新，不需要新增宿主实现。当前可调用工具未提供该运行实例的刷新操作；此前请求的插件停用/启用已暂停，因为下文确认的插件漏判仍须修补；这个历史阶段没有要求用户操作，后续新增 PostToolUse 的信任要求见末节。没有修改宿主、重建业务任务、提高名额、删除历史日志或更改共享预算。完整验收仍为失败/未完成状态。

本候选完整真实模型检查的首轮在 13 个控制回合后停止：前四组通过，守卫样本把“原生 exec_command、不要用其它工具”理解为禁止必要的代码模式包装，未尝试命令。该用例没有获得拦截证明，报告保留失败。驱动已明确授权单次标准包装；另修补了校验器只匹配拒绝文字的误判，加入精确命令、严格转发及原生错误格式检查，4 项本地校验器测试通过。先前成功的真实代码模式对照已按新规则回查仍通过。

第二轮被严格校验器拒绝：模型给命令多加了转义层。真实 Hook 虽拒绝了这条变形命令，仍不能算指定 canary 通过。驱动将命令简化为单行 `printf` 写入专属临时文件，并直接给出 JavaScript 源码；没有放宽校验。随后单独的真实代码模式守卫对照通过，4 个控制回合、原 outcome 不变、无新 Start、进程组清理成功。驱动修补不改变安装插件的源码摘要。

该候选完整预设真实模型用例随后通过：18 个根控制回合、7 组用例，清理成功，报告为 `router-residency-semantic-8G1UIo/semantic-report.json`。这只证明已执行用例，原任务两个漏拦截结果和新增操作漏判不能被覆盖。

## 新增反例：进程仍在运行却已 ready

当前同一候选、真实模型和受信 Hook 下，驱动让 child 执行一次普通代码块：把真实 `exec_command` 结果传给 `text()`，再追加一次普通 `text()` 输出。专属进程打印带随机标识的 PID 后等待外部释放文件。外层代码单元返回，child 给出最终回复，但根驱动尚未创建释放文件。

| 观测 | 实际结果 |
| --- | --- |
| 测试 root / child | `01a0828e-b51d-7621-922b-eb75ce84ab92` / `01a0828f-2c23-70c3-a266-f64e434c63ed` |
| Router 判定 | `stageClosure=ready`，`pendingCalls=[]`，`pendingOperations=[]` |
| 独立操作系统检查 | 专属测试进程存活，随机标识匹配，释放文件不存在 |
| 原因 | `forwardedToolCall` 只接受固定单一转发形式；多一个输出使其返回空，`observeForwarded` 随即跳过，外层完成时漏掉内部进程 |
| 清理与 outcome | 驱动创建释放文件，实际进程退出后才记录 failed/tooling；自己的进程组亦已退出 |

反例报告、驱动、日志摘要已存入本页配套 JSON 的 `hotMcpCandidate.opaqueOperationCounterexample`。归档后的真实 child 日志仍可读取；反例发生前的截面和随后清理必须区分，不能用清理后的退出事件倒推它在 ready 时已经结束。

本次源代码复核使用本机官方 `rust-v0.153.4` 对象 `3d2ee51ca2d5db578f328aa75e20aa22c0197c9a`，没有修改宿主：

- `core/src/tools/handlers/unified_exec/exec_command.rs:504`：原生命令使用 Bash PreToolUse，参数为真实命令。
- `core/src/tools/context.rs:390–411`：未结束进程不产生 Bash Post；终态 Post 使用原命令调用 ID。`registry.rs:674–692` 还区分工具成功返回和处理错误，不能假设每个 Pre 都必有 Post。
- `core/src/tools/handlers/unified_exec/write_stdin.rs:124–138`：`write_stdin` 明确不产生 Pre，可在终止时补原命令的 Post；所以匹配器 `.*` 不等于全工具强制沙箱。
- `rollout/src/policy.rs:93–109` 与 `protocol/src/items.rs:200–273`：当前分页日志保存独立命令终态。实际释放后，child 日志确实新增 `CommandExecution`，调用 ID 为 `exec-a3823e2e-6bd5-46db-8e80-feb9e1629b09`、过程句柄 `4388`、exit code 0；不需要外层再打印它。

另一条早期探针在 Node 内写 PID 文件时得到 `EPERM`，原生返回 exit code 1，没有进入持续等待；它不是此反例，更不是“进程从未启动”的证据。其当前日志也没有独立命令结束项，说明不能只取一种日志事件作为跨情况的唯一依据。启动前拒绝、工具层创建失败和结果丢失须分别补验。

修订方案采用现有回调与实际终态记账，正常路径自动核验；缺失证据时按明确标准由主任务逐项核实并保留责任。具体标准见[方案第 5.1 节](../HOST-CAPACITY-LIFECYCLE-REPAIR-PLAN.zh-CN.md#51-正常自动核验与异常核实标准已接受)。该账本及异常核实机制尚未实现，V23—V25 未通过，完整验收仍为 false。

根结束检查随后提示 Erdos 移交责任尚未处理。根已用 `read_disposition` 读取原始记录，再次确认 child completed、专属 canary 文件已不存在、原 outcome 摘要未变。该责任在 revision 3 明确登记为 deferred，责任人仍为 `/root`，恢复条件为明确异常核实标准、实施插件补强并回到实际旧缓存路径重验。`resolve_requirements` 的计数表示登记了一项处置，不表示修复完成；`pendingStageWork` 继续保留它，没有取消目标或把独立审查改记为主任务完成。

## 命令账本候选已安装，真实操作验收等待一项 Hook 信任

候选 `0.4.0+codex.20260908204652`，源码及安装摘要均为 `7db7a4735ce81bf9060b777cff329593216ccbfbecddf224fc517bfdb6544c51`。新增 `child-command-journal.mjs` 使用已有 Bash Pre/Post 记录命令身份和摘要，普通 JavaScript 包装不再决定是否存在运行操作；当前分页原生日志中的命令终态按 child、原调用及开始回合关联。命令非零退出仅表示终止，业务结果仍由根核验。重复事件不会重新打开已结束调用；不一致的回执保留冲突。

两条新回归先复现 ready 漏判，再通过修补。迁移复核发现，仅在新建数据库时创建账本会漏掉已有 v7 数据库；对此同样先建立失败用例，再增加 v8 迁移，验证根状态保留。新增数据库触发器防止旧运行时仅填写旧验证字段便绕过命令账本；当前验证与命令更新在已有事务边界内衔接。新的开始/终态使旧验证失效，重复的相同回执不制造新版本。

本候选全量测试 414 项，413 通过、0 失败、1 项 Windows 专属检查跳过；232 个策略案例无不匹配，验证脚本及差异检查通过。Mac 原位安装、受管来源刷新和既有桥接探针通过；实际旧 MCP 的 diagnose 返回 runtime/active/requested 均为本候选，数据库版本与支持版本均为 8，历史隔离记录未删除。不同开发中间态的失败日志保留，但不作为本候选通过证明。

新增[原生操作驱动](../../scripts/probe-residency-operations.mjs)已准备三组实际模型场景：普通多输出、两个并行命令、预期非零退出。运行在预检即返回 `HOOK_TRUST_REQUIRED`：0 个模型回合、未创建测试根或 child，自己的 App Server 进程组已退出。因此还没有证明真实命令自动收尾通过。首份报告的 `realModels` 字段表示预定模式，配套 JSON 已明确 modelTurns=0；驱动已将预定模式与实际启动分别报告。

原生 `hooks/list` 确认只剩 PostToolUse 为 modified，当前定义新增 Bash 完成回调，哈希为 `sha256:dbd3aa6b50b12bf195f7ee6d5843122d2e47d77dc2185be2b105c06db651ba14`；其余六项均 trusted 且 enabled。已请求用户在现有 `/hooks` 审核这一项，依据仓库 AGENTS 的明确一次性信任边界，没有修改信任数据库或使用绕过参数。当前任务的独立审查路由另返回 `HOST_HOOK_SET_MISMATCH`，未创建 child；不能将新库存检查冒充旧任务已刷新。

异常人工核实机制、共享预算选择、旧任务维护守卫与实际后续委派，以及最终候选完整原生验收仍未完成。此前“自动账本尚未实现”是旧候选复核时的状态；自动部分现已实现并安装，但不能据此清除这些剩余条件或标记完整修复。

## 本轮方案复核：异常入口仍需补齐

重新读取原 Erdos 的 revision 3 处置，确认守卫问题仍由根承担并明确延期，未取消、未改写旧 outcome。当前 stageClosure 为空，待核实责任保留；没有为了结束本轮重复写入一份同义处置。

代码核对确认，当前 `manage_stage` 不含执行异常核实动作；有待定命令时，closure 会提前返回 pending，而维护验证又要求 ready。因此“主任务核实后收尾”仍只有设计，没有完整工具入口。已在方案第 5.1.1 节列出逐操作入口、阶段与操作双快照检查、原始引用、幂等记录和重新计算 closure 的实现约束。

同一原生版本源码另有正常完成但缺 Bash Post 的路径：exec_command 被 apply_patch 处理器接管后，返回结果的 hook_command 为空，结果适配器不生成该 Post。此项是源码证据，尚未在当前候选的真实 child 上重放；不把没有回调推断为未执行或仍运行。方案增加 V26/V27，分别覆盖这条实际写入路径及异常核实过程中的版本竞争。

本轮只修改方案与证据文档。再次核对候选源码和安装摘要相同，实际 MCP 仍运行 `204652`，数据库 v8 健康；差异及操作驱动语法检查通过。全量本地测试沿用摘要相同候选的 413 通过、1 跳过记录，未为文档改动重复运行。21:06 UTC 的新 hooks/list 仍仅 PostToolUse 为 modified，其余六项 trusted；未绕过信任或重复启动已知会被预检拒绝的验收。独立审查路由仍返回 `HOST_HOOK_SET_MISMATCH`，没有启动 child；本轮复核为主任务复核。

## 当前候选：补丁终态自动收尾 `212209`

候选 `0.4.0+codex.20260908212209` 已在 Mac 安装，核心源码与安装摘要均为 `e2d24b5efc17bb49a7b9293de9172aad8806115034854035e4d5ed51c6d25216`。实际旧 MCP 返回 runtime、active、requested 均为本候选，数据库 v8 健康；旧隔离历史未删除。

本次进一步核对 `codex-rs/core/src/tools/handlers/apply_patch.rs` 和 `tools/events.rs`：原生接管 exec_command 的补丁后，使用原调用 ID 发出 FileChange 终态。根任务在专属临时目录实际执行了该命令，确认预期内容，并从本根原生日志读到 `exec-a4c46184-c096-45e0-a65b-d818a376b8c9`、准确回合和 completed 事件。用该原生事件对解析器回放，未结操作由 1 变为 0；回放的命令账本条目为构造值，所以只证明事件解释，不冒充 Router child 的完整回调链。临时文件和专属目录已精确清理。

5 条新失败回归先证实旧解析器没有处理此终态；修补后 49 项针对性检查全部通过。新逻辑只关联准确 child、原调用、开始回合和已有记录，接受 completed/failed/declined 的真实结果摘要，不伪造 Bash Post、退出码或“未执行”结论。错误身份、缺字段、非终态、模型打印的同名 JSON 均不能解除责任；重复结果幂等，冲突继续 pending。集成测试核对 pending 可转 ready 并记录唯一 outcome。

本候选全量 420 项测试，419 通过、0 失败、1 项 Windows 专属跳过；232 个策略案例零不匹配，4 项验收校验器检查、语法、校验脚本和差异检查通过。新真实模型驱动增加补丁用例，仅在自己的临时工作目录允许写入，要求真实 Bash 开始记录、FileChange 终态、预期文件及唯一结果全部成立；该用例尚未执行。

21:26 UTC 的原生 Hook 清单仍只有同一 PostToolUse 为 modified，哈希仍为 `sha256:dbd3aa6b50b12bf195f7ee6d5843122d2e47d77dc2185be2b105c06db651ba14`，其余六项受信。本次补丁分支没有新增 Hook 定义或信任项，也未重复运行已知受阻的模型验收。没有活跃测试进程遗留。异常人工核实机制和共享预算选择仍待用户答复，旧代理守卫与独立审查仍未完成；完整验收保持 false。

## 用户接受后的修补与安装 `004322`

用户回复“已信任，接受”后，七项 Hook 均确认 trusted/enabled；异常核实中的根语义判断和实际磁盘余量保护也取得明确授权。先在未变更的 `212209` 上完成四组真实 child 命令验收：普通多输出、两条并行命令、实际退出码 7、被原生接管的补丁。进程存活时保持 pending，自然终态或补丁结果出现后正常结算，自己的进程组全部退出。该结果只归属于 `212209`。

新候选 `0.4.0+codex.20260909004322` 的源码和安装摘要均为 `3e036510a5d5578d179ddc328073988f85a22e38b17d2134b74b395d3eca0bd7`，已在 Mac 原位安装并刷新托管来源。真实旧 MCP 的 active/runtime/requested 全部匹配，数据库 v9 健康，历史八条运行时隔离记录保留。429 项全量本地检查中 428 通过、1 项 Windows 专属跳过；232 个策略案例零不匹配。

- `read_operations` 与 `reconcile_operations` 允许在 pending 时逐操作核实。原命令、子任务原始记录和根的实际核实结果按身份与摘要关联；拒绝伪造引用、任意打印数据、过期消息版本和命令快照。未明项保留来源、责任人、下一步与恢复条件。核实记录加密追加，重新计算 closure 后仍须验证当前结果；不直接创建 outcome。
- 二次维护保留已有要求、处置、日志增长计量和原 outcome；新意图必须对应新的有界收集，并获得新 followup/final。不能把旧验证令牌用于新一轮，也不能借此给旧 child 分派新业务。
- 默认存储准入按实际余量保护，保留每条 256 MiB 预留、全局四条未结上限和扣预留后 4 GiB 空闲底线。累计审计日志超过 1 GiB 不再永久拒绝未来委派；没有提高原生名额、删除日志或清除其它任务预留。
- 已审阅的历史 matcher 升级可进入无工具原生自检，仍须实际回调通过。改变命令或未受信定义继续拒绝。

新候选的真实异常验收通过：命令使用不存在的工作目录，真实原生回执为创建失败，只有 Bash Pre 而没有 Post。先登记 unresolved 及根责任，再由原根执行实际检查命令，核对原错误、缺失目录和未产生的唯一测试文件。伪造引用被拒绝，核实重复提交幂等，核实后仍未伪造 Post；记录唯一结果后，同根成功创建并完成下一阶段。共五个根控制回合，拥有的进程组已退出。

同根 100 阶段协议验收也通过：101 个唯一 outcome、101 个受管理 child、706 次原生调用、零容量拒绝，累计 12,931,072 字节，保留最近 64 条尝试而不丢 child 责任；最多观测到根加三名 child。固定模型响应只证明协议和收尾行为，真实模型质量另行验证。该驱动的进程组已退出。

原旧任务的独立审查前自检尚未通过：实际创建了 `/root/router_e5a8bf84ed98c596f52e524beed4c91a`，但未触发消费票据的 Pre 回调，child 因而拒绝工作。现有原生恢复命令逐项核对原根启动、child 单轮无工具最终回复及完成记录，才释放本次预留；没有伪造缺失的 Pre/Post/Stop，也没有写入不存在的验证 outcome。独立审查未执行。已请求在应用设置中将现有插件停用后重新启用以重载定义；这不是新增宿主能力或重新申请信任。原 Erdos 守卫责任仍按 revision 3 明确延期保留，尚未标为完成。

当前候选的其余三个原生套件随后全部完成：

| 套件 | 当前候选的实际结果 | 证据边界 |
| --- | --- | --- |
| 真实模型语义 | 18 个根控制回合、7 组用例通过；补充遗漏被拒绝并纠正，运行中的补充及取消取得实际操作终态，缓存 child 的精确 canary 被守卫拒绝，延期责任跨回合可恢复 | 新加载任务的缓存守卫，不替代旧 Erdos/Ptolemy |
| 真实命令 | 6 个根控制回合，普通多输出、两条并行命令、退出码 7、原生接管补丁四项通过；运行时分别确认 1/2 个专属进程存活且 closure pending，随后自然退出，无额外轮询；补丁的真实文件及 FileChange 终态匹配 | 未伪造 Bash Post，未重试失败命令 |
| 满额恢复 | 39 次原生调用；三个缓存代理均积压有效补充时确实被容量拒绝；一个代理处理原要求后，同根同阶段的新票据委派成功；其余补充随后均收尾 | 压力样本为非 Router 原生 child，使用固定模型响应；候选 Router、受信 Hook、原生拒绝和恢复均真实 |

三个套件与此前异常、100 阶段套件的摘要均匹配 `004322`，各自拥有的进程组已经退出，命令套件另外核对全部专属进程不存在。报告原文、路径与 SHA-256 已追加到配套 JSON 的 `acceptedExceptionCandidate`，未覆盖旧候选结果。

原任务冻结的工具清单可经当前安装的 stdio 桥接调用 `read_operations`：正确读取原 Erdos、revision 3 和空操作列表；对应的延期责任仍保留。证据整理路由因原任务资格失败而返回 continue，没有产生独立审查结果。当前只剩原旧任务定义重载后的守卫、原 Hosted 任务后续委派、所需独立审查及其责任核验尚未完成，完整验收保持 false。

## 用户重新启用后的原任务复验

用户确认“已重新启用”后，本修复原任务实际记录 PreToolUse 与准确身份，工具清单也已包含新版操作核实入口。新资格路由 `09458f4e…` 的真实 Pre/Post/Start/Stop 与完整无工具原生日志审计通过，记录唯一 passed outcome。已通过的资格随后从旧、新两个 shell 根收敛到实际观测的新根，触发一次新的资格 `0291ba19…`；两次的 runtime/configuration 摘要一致，第二次同样核验通过。后续路由正常进入实际 gpt-6-astra / xhigh 审查 `3ca19999…`，没有陷入连续资格循环。

原 Erdos 在新一轮 `begin_maintenance` 后实际尝试唯一指定 canary，严格校验器在原生日志匹配到准确命令及 Router PreToolUse 拒绝；当前测试文件和上次测试文件均不存在，原 outcome 前后摘要相同。真实最终回合 `01a083df-8d3b-7471-b15d-8e4f05112c4a`、最新 Stop、三项后续输入的处置均已核对，revision 5 维护 verified。诊断窗口内有 Pre 与 Stop，无新 Start；到 Stop entry 的日志前缀为 61,383 字节，仍可容纳允许的最大 4,096 字节记录而不触及 65,536 字节上限。后续诊断达到大小边界，不用于推断更晚事件；诊断已关闭，记录保留。

原 Hosted 任务正在执行其 Ptolemy 守卫与同根新委派验收；此回合明确禁止恢复生产流程。当前独立审查尚未返回，原根的修补责任保持 deferred，完整验收仍为 false。本轮原生资格、Erdos 守卫和具体摘要见配套 JSON 的 `acceptedExceptionCandidate.afterPluginReload`。


## 独立复核补齐与最终候选 `030332`

`024100` 修复了历史不透明代码模式、迟到轮询句柄复用、只读采用、重复维护责任覆盖、精确 Pre 拒绝消息与维护准入顺序。全量 449 项中 448 通过、1 项 Windows 专属跳过；真实语义 8 项、真实命令 4 项和实际创建失败异常路径通过。这些结果仍属中间候选：在实际独立审查结算时发现记忆引用分离使 Stop 比对失败，故未判为完整通过。

真实子代理原生日志保留了两种结果：raw response_item 包含 `<oai-mem-citation>`，native AgentMessage 将相同引用保存到独立 metadata，task_complete 和 Stop 使用保留原末尾换行的正文。最新实例的完整摘要为 `58d00461308c6e83adcd00b986b21ed939a5f9d87eb3ad8c9b6c48d4fcad2554`，真实 Stop 摘要为 `e71415a2ae07e90bc567faac7e7986c97a283208958c6199f507e69984733194`。此前反复补最终回复不能解决这个差异；本次未修改原始日志或手工添加 Stop。

新候选 `0.4.0+codex.20260909030332` 核心摘要 `254116a354cec4ce1b0e1bf5dd9f334f86ee325d1415c5925d86c74ad3574ad5`，已在 Mac 安装，实际旧 MCP active/runtime/requested 均匹配，数据库 v10，七项 Hook 仍受信。新的匹配分支核验相同 child、turn、非空 message id、严格正文内容块、唯一完整引用及 metadata、晚于结果的相同完成文本；完整 raw 摘要和新的原生证明共同进入结算 token。独立审查又抓到 raw 内容块形状和空 id 漏检，均修补后通过 24 组纯内存正反例。代码审查和根运行的验收分别归属，不冒充审查者亲自跑过本机套件。

正确插件 cwd 下全量 **450 项，449 通过、0 失败、1 项 Windows 专属跳过**；232 项策略案例零不匹配，4 项验收校验器、插件校验与差异检查通过。此前根从仓库 cwd 误调用测试，单项报找不到 `scripts/hook.mjs`；该命令错误保留为失败日志，未修改测试绕过，已按 package.json 的插件目录命令重新完整运行。

原修复任务中的实际独立源码审查完成。根逐项检查只读代码及真实原生命令终态：47 个外层调用、98 条原生命令；37 个旧不透明操作通过准确原始引用与根执行的核实回执处置。真实 Stop 随后正常匹配，stageClosure revision 13 ready，记录唯一 passed outcome（seq 1282）。操作核实只证明执行终态，不覆盖业务结果；最初发现与修正历史完整保留。

原缓存 Erdos 在最终候选下再次实际尝试唯一 `/tmp/router-final-erdos-guard-2071a203-5d68-4d37-94a7-abc96d12e5dc`，获得准确 Router Pre 拒绝；当前和历史 canary 均不存在。根按每个原调用区间核实 34 条真实命令终态及两次精确拒绝，15 项旧未明操作得到逐项处置。维护 revision 8 verified，原 outcome 按同一完整投影前后不变；既有 deferred 修补责任暂保留，待整体验收完成后用 resolve_requirements 单独解决。

同根正常路由也已复验：新版本一次无工具资格通过（seq 1284），随后实际启动 gpt-6-astra / high 独立证据审查。030332 的真实语义 8 项、命令 4 项和实际异常路径已通过，三个驱动拥有的进程组全部退出。100 阶段协议、实际满额恢复、原 Hosted 的 Ptolemy 守卫与新委派及最终证据审查仍进行中，完整 Mac P4 暂保持 false。JSON 的 finalReviewedCandidate 保存当前来源、摘要和具体结果，所有旧候选的失败与通过均保留。

## 最终 Mac 验收完成

后续全部验收及责任结算已完成。当前实装仍为 `030332`，核心摘要仍为 `254116a354cec4ce1b0e1bf5dd9f334f86ee325d1415c5925d86c74ad3574ad5`；补充专项只修改测试和仓库外层驱动，没有更换通过验收的产品实现。配套 JSON 的 `fullAcceptancePassed` 与 `finalReviewedCandidate.fullAcceptancePassed` 现为 true，范围限定本次 Mac 修复。

| 证据 | 最终结果与边界 |
| --- | --- |
| 本地检查 | 原全量 450 项：449 通过、0 失败、1 Windows 专属跳过；232 策略案例零不匹配，4 校验器检查通过。之后新增回滚专项另行通过，不混入原计数。 |
| 同根 100 阶段 | 101 个唯一 outcome、101 个 child、711 次原生调用、50 次 followup、5 次受控最后响应窗口；零容量拒绝，累计 13,537,280 字节；64 条历史尝试裁剪后责任仍保留。固定响应不冒充模型质量。 |
| 真实模型与命令 | 8 组语义用例（含资格）、4 组命令用例和实际创建失败核实通过；补充遗漏先拒绝再纠正，活动进程真实终态后结算，接管补丁核对实际文件与 FileChange。 |
| 实际满额恢复 | 36 次原生调用；三个已完成 child 均有有效补充时真实拒绝，处理一个后同根同阶段获得新票据并完成，其余补充均保留及落实。 |
| 双根与 LRU | 同一个 AppServer、同一 Router 数据库，两个原生根同时各有三个驻留 child，分别拒绝和恢复。旧 child 从驻留清单实际淘汰后，以同一持久身份重新加载；两次后续要求保留原成果，各新增 65,536 字节，重复读验/verify 不重复记账，另一根清单与预算不变。共 98 次原生调用。 |
| 回滚保留责任 | 独立临时副本触发真实 launcher 隔离与回退；默认兼容核心专项及真实旧 `024100`（摘要 `81eaff4a…`）专项各 1 项通过。回退后另启进程，加载被选中旧实现自身的 RouterStore/service，核对延期责任、未结束操作引用与历史 outcome，旧 token 仍被拒绝。证明实际兼容数据库 v10 的上一实现，不承诺任意旧 schema 回滚。 |
| 原 Hosted 任务 | 原根完成 Ptolemy 精确守卫及新的 gpt-6-astra / xhigh 独立审查，最新 citation/Stop 自然闭合并唯一结算。44 条旧 outcome 的同一摘要由本根只读重算确认不变，两项业务延期继续由原根保留。没有执行生产流程。 |
| 独立审查与本根结算 | 代码审查 seq 1282、最终证据审查 seq 1286 均为真实代理、当前 ready 令牌后的唯一 passed。Erdos 历史守卫与操作核实通过后，原根延期修补责任明确完成，revision 9，旧 outcome 不变。 |

独立证据审查发现 V11 的实际 LRU 重载、V15 的回滚责任读取、V19 的共享数据库双根隔离原先证据不足，以上专项补齐后才通过，未缩减原矩阵。V09 分别记录合成延期要求的跨轮读取/意图变更取消，以及本修复原根责任在多次用户输入和运行时变化后的实际恢复与完成；不把前者称为重新执行合成业务。完整 V01—V28 映射位于 JSON 的 `finalReviewedCandidate.coverage`。

双根驱动首轮使用了不合法的大写 agent 名字，原生拒绝后脚本解析失败；修正后 v2 的隔离/重载已通过。v3 追加字节断言错误地要求每次文件已分配空间必然增长，保留为失败；v4 使用明确有界的 64 KiB 固定响应输入压力，按真实文件高水位差值核对，并验证重复调用幂等。没有改产品代码去迎合断言，失败和成功轮的专属进程组都已退出。

最终原根状态为 `pendingStageWork=[]`、`pendingOutcomes=0`、`stageClosure=null`、`delegationGate=available`。实际 MCP 的 runtime/active/requested 均为 `030332`，DB10 健康，源码与缓存摘要相同。所有验收驱动的专属进程组退出，原任务 canary 不存在；此前故障历史与八条运行时隔离记录保留。Mac P0—P4 完成，无需用户再次重启、启用或授权。以上为提交前的验收记录，不宣称 Windows 原生验收或 Hosted 产品生产发布通过。
