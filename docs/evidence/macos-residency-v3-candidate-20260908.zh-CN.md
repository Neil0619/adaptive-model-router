# 子代理驻留修复 v3：Mac 候选安装与待验收记录

历史记录：本页保留早期候选及当时的信任状态。当前 Hook 已受信，信任请求已失效；最新安装与真实验收以[9 月 9 日原生验收](macos-residency-v3-live-20260909.zh-CN.md)为准。本页早期候选在当时尚未通过完整验收。

对应[修复方案 v3](../HOST-CAPACITY-LIFECYCLE-REPAIR-PLAN.zh-CN.md)；机器可读摘要、安装源、逐项 Hook 哈希及执行日志摘要见[证据 JSON](macos-residency-v3-candidate-20260908.json)。

## 已完成

- 候选版本：`0.4.0+codex.20260908152938`。Mac 安装源与工作区核心源码及 MCP/Hook 入口摘要一致，保持已有任务。
- 正常生命周期：补充消息主动进入同阶段续轮；旧 Stop 或旧验证结果不能结算新要求。
- 遗留维护：保留原 outcome、部分结果和要求出处；明确取消、替代、延期或转交。维护代理的业务工具被守卫禁止，已知运行操作只允许通过原生等待工具查询。
- 缺失回执：核对原生发送调用及实际结果，支持根与同树其他发送者，拒绝其他任务的证据，不盲目重发。
- 容量恢复：保留历史拒绝审计，改为本次暂忙及后续重新准入；同阶段的恢复尝试有界，不增加原生名额。
- 安装兼容：第一次实际安装发现旧 MCP 清单比较拒绝增量接口，安装器回滚。修复后，真实旧清单回归、运行版本切换及回滚检查通过，最终安装成功。

| 检查 | 当前结果 |
| --- | --- |
| 全量本地测试 | 387 项：386 通过，0 失败，1 项 Windows 专属检查跳过 |
| 校验器、差异空白检查 | 通过 |
| 策略评估 | 232 个案例，无不匹配 |
| 安装及旧入口、Hook 脚本、stdio 桥接探测 | 通过 |
| 当前原生 Hook 清单 | `HOOK_TRUST_REQUIRED`；两项 `modified`，五项 `trusted` |
| 启用 Router 和受信 Hook 的同根 100 阶段 | 未执行 |
| 真实模型补充要求、取消/延期、维护守卫及结果质量 | 未执行 |
| 三份历史积压导致实际满额后的同根恢复 | 未执行 |
| 原“完成 Hosted 验收并上线生产”任务恢复、独立候选审查 | 未执行 |

固定模型响应的协议探针和原生机制对照不替代真实模型质量验收。本次没有执行业务生产写入，没有发送宿主接口请求，也没有提交代码。

## 9 月 9 日复核

安装候选及核心源码摘要未变化。真实模型验收驱动和[执行说明](../../scripts/residency-acceptance/README.md)已补齐已有用例的核验步骤，3 项结果校验器测试及语法、差异检查通过。最新原生预检查仍以 `HOOK_TRUST_REQUIRED` 中止，模型回合数为 0，临时宿主进程组已确认退出；这不是模型验收通过。

本任务原生 `get_route_status` 和安装后的 stdio 桥接读取均成功，委派锁可用、待 outcome 数为 0、`stageClosure` 为空、没有待处理阶段工作。此前旧 MCP 进程曾拒绝工具合同；本次只确认原生状态读取已经成功，不据此宣称全部旧任务委派入口通过。记录整理的路由使用桥接入口，返回 `continue / HOST_HOOK_DISPATCH_NOT_OBSERVED`，没有启动子代理。原任务真实委派恢复仍属于 P4 待验收项。

当前原生预检查及源码核对的机器可读回执见[9 月 9 日复核记录](macos-residency-v3-preflight-20260909.json)。

## 当时的信任要求（已完成，无需重复）

当时需要通过 `/hooks` 审阅以下两项修改；本轮原生验收已经确认它们受信：

| Hook | 变更用途 | 当前哈希 |
| --- | --- | --- |
| PreToolUse | 覆盖消息登记及维护代理的工具守卫；只有已知句柄的无输入等待例外 | `sha256:0df4bff3f5331889e6c4a65acae10b2ea5d6630c8f91e9c346f2d3a68c18084c` |
| PostToolUse | 记录 spawn、send、followup、list、interrupt 的真实返回 | `sha256:a22d835866e5bdf416dfc1c28142c4ee3a7e93af8ee3ef472f60960c9aa64b92` |

项目 [AGENTS.md](../../AGENTS.md:46) 明确规定："Ask for that one-time approval only when the current Hook hash is not already trusted"。该条件曾成立，现已完成；无需再次回复或重新信任。它是既有插件信任流程，不是新增宿主接口。

信任后，由代理核实真实 Hook 执行和当前任务桥接，再完成上述原生验收并记录结果。任一验收失败都继续定位和修复插件，保留已接受要求和操作证据；尚未通过的阶段不得标成完成。
