# macOS 宿主数量上限拒绝恢复验收

本机已加载 `0.4.0+codex.20260908083042`。真实故障已恢复，独立审查与原生委派验收通过。
完整脱敏数据见 [JSON 证据](macos-host-capacity-recovery-20260908.json)。

## 故障与修复

2026-09-08 13:56（北京时间），任务「完成 Hosted 验收并上线生产」在先前
40 次委派均已收尾后，收到原生错误 `collab spawn failed: agent thread limit reached`。
这次五个启动参数完整，票据已消费，但没有 PostToolUse 回执和子代理身份；
已有 `failed/tooling` 结果无法单独解除锁。日志未暴露宿主配置的数量上限。

新恢复分支核实原生调用、精确错误、可信 Hook 输入摘要和完整父任务投影，
以有界逐行审计处理长任务日志，并在事务内复核票据、路由分类、资格和已有结果。
通用错误、重放、迟到的子代理证据和变更的状态均不能解锁。
MCP 的普通 failed/tooling 结果记录会自动审计此类失败；既有记录也可由安装目录的
原生恢复命令检查和应用。只读状态查询不会修改恢复状态。

## 真实任务结果

目标任务在热更新后自行遵循新技能，使用当前安装目录执行检查和应用，
于 16:34:18 完成恢复。此次审计了 214,850,710 字节的原生父日志。

- 释放 256 MiB 预留，撤销失败票据，原来的失败结果逐字段保持一致。
- 原始 PostToolUse、SubagentStop、no_child 字段仍为 0，没有补造生命周期记录。
- 另外两条历史未完成记录保持不变。
- 后续原生路由返回 `continue / HOST_AGENT_LIMIT_REACHED`，没有再次分配子代理。
- 主任务仍为 `gpt-6-astra`。容量标记仅作用于该任务，其他任务的正常委派已实际验证。

## 验证

- 全量：352 项，351 通过，1 项 Windows cmd.exe/PowerShell 检查在 macOS 跳过。
- 路由规则：232 个用例，无差异；插件及仓库校验通过。
- 安装目录：独立审查者和主任务分别验证 12 项容量恢复测试，均通过。
- 独立 `gpt-6-astra / high` 审查未发现阻塞问题，原生 Pre/Post/Start/Stop 关联完成并记录结果。
- 新版本原生资格验证通过；热更新移除一个旧启动目录绑定后，资格刷新也通过。
- 源码、受管安装源和活动缓存运行时摘要一致；7 个 Hook 的定义摘要、启用及信任状态不变。
- 本机配置仅刷新 Router 的受管 marketplace 来源；346 个现存无关文件保持不变。
- 当前修复任务的路由锁可用，待记录结果为 0。

## 边界

修复解除 Router 的遗留占用，不提高宿主自身的子代理数量限制。受影响任务后续由主任务继续，
不会因经过一段时间或调用方宣称工具可用而再次预留；恢复宿主容量需要独立的可信宿主证据。
审计限定已验证的 Desktop 0.153.4 格式，父日志上限 512 MiB、单条记录 16 MiB、总记录数 100,000。
未知格式、超限或冲突证据保留原有安全边界。本次没有执行 Windows 原生验收。
